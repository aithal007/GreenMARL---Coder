# Extras: TRL GRPO + OpenEnv-style tool training (install on top of requirements.txt)
# Example:
#   pip install -r requirements.txt
#   pip install -r requirements-train.txt

# Pin loosely — adjust if your stack conflicts (Colab: usually fine)
trl>=0.20.0
datasets>=2.19.0
peft>=0.14.0
matplotlib>=3.8.0
openenv-core[core]>=0.2.1
